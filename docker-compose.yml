services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "${PORT:-5000}:5000"
    volumes:
      - ./instance:/app/instance
      - ./uploads:/app/uploads
    env_file:
      - .env
